spring:
  application:
    name: ecom-backend
  datasource:
    url: jdbc:mysql://localhost:3306/ecom_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: "15032004"
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    default-property-inclusion: non_null

server:
  port: 8080

app:
  security:
    jwt:
      secret: "change-this-secret-to-a-long-random-base64-or-plain-text-value-when-deploying"
      expiration-ms: 86400000
  cors:
    allowed-origins: "http://localhost:5173"
